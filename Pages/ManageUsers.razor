@inherits BaseDbComponent<ManageUsers, DB>
@page "/manage-users"
<PageTitle>User Management</PageTitle>

<PageWrapper Title="User Management">
    <RadzenCard>
        <UserDataGrid ShowEditColumn=@CanEditUsers/>
    </RadzenCard>
</PageWrapper>

@code {
    protected bool CanEditUsers { get; set; }

    protected override async Task OnInitializedAsync() {
        base.OnInitialized();
        var identity = await GetAuthStateAsync();
    }
}
