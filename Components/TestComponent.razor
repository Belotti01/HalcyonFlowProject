@inherits BaseComponent<TestComponent>

<RadzenCard>
	<RadzenButton 
		Text=@Translate("Database Options") 
		Click=@OpenDatabaseSettings
	/>
	<RadzenCard>
		@Str
	</RadzenCard>
</RadzenCard>

@code {
	public string Str { get; set; } = "";

	protected async Task OpenDatabaseSettings() {
		var res = await Dialog?.OpenAsync<DatabaseSettingsComponent>(Translate("Database Options"));
	}
}
